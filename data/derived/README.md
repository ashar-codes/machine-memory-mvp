# Derived subsets

No public subset has been generated. Future normalization must include source row/key mappings, timestamp interpretation, exact selected assets and coverage, transformation version, rejected rows and pre/post counts. Never claim a 7-day count represents lifetime recurrence. Separate synthetic enrichment from public observations.
