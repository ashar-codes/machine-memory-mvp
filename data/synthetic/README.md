# Synthetic demo data

The authoritative runnable fixture is `../../supabase/seed/`. Demo site/assets, `PITCH-HYD-214` and narratives are fictional. Every seed record carries `record_origin = 'synthetic_demo'`. Do not present the site as Zephyr or fictional histories as Penmanshiel observations. Interactive saved resolutions use `user_demo`, regardless of the validation checkbox.
