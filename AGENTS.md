# Machine Memory agent instructions

Read PROJECT_CONTEXT.md, ARCHITECTURE.md, docs/API_CONTRACT.md and docs/AGENT_OWNERSHIP.md before implementation. Current repository files are authoritative. The hybrid retrieval and synthesis pipeline and the investigation workspace are implemented; live database, model and browser verification are not. Check the checkpoint table in PROJECT_CONTEXT.md before claiming any gate passes.

Security, correctness, simplicity and maintainability come first. Work only in your assigned directory. Shared types/schema/API are frozen v1; report genuine blockers to Architect before altering contracts. Never edit another owner's files while they work. Delegate only bounded independent tasks that the user or ownership plan authorizes.

No secrets in frontend, commits, logs or examples. Never use an existing E-Set production project as the Machine Memory database. No-login server remains loopback-only. No model-generated SQL/code execution or equipment-control endpoints. All data must retain origin. Demo validation flags confer no authority.

After each implementation section run relevant tests, typecheck/lint/build, inspect diff/security/integrity, then commit a verified checkpoint. Do not report unrun live gates as passing. QA integration begins after owners finish; root lockfile modifications serialize through Architect.
